name: My First Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Say Hello
        run: echo "Pipeline работает! Коммит задеплоен."
      
      - name: Show date
        run: date
      
      - name: Show files
        run: ls -la
